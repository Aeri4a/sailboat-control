import React from 'react'
import ReactDOM from 'react-dom/client'
import AppSource from './AppSource.tsx'
import './index.scss'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppSource />
  </React.StrictMode>,
)
